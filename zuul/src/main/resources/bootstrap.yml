spring:
  application:
    name: zuul
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
      fail-fast: true
      label: master
      profile: dev
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/